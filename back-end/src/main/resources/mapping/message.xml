<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chatonline.Dao.MessageDao">

    <insert id="InsertMessage" >
        insert into message(sendid,reciveid,sendtime,messagetext,texttype)
        value(#{sendid},#{reciveid},#{sendtime},#{messagetext},${texttype})
    </insert>
    <select id="FindMessage" resultType="boolean">
        select count(*) from friendmessage where sendId = #{sendid} and reciveId=#{reciveid}
    </select>
    <delete id="DelMessage">
        delete  from friendmessage where sendId=#{sendid} and reciveId=#{reciveid}
    </delete>
</mapper>